conexao = {
    host     : 'localhost', // O endereço da conexão (localhost).
    user     : 'root',     // O nome de usuário do banco.
    password : '',        // A senha do banco.
    database : 'login'   // O nome do seu database.
}
    
module.exports = {conexao}
